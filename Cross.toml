[build]
build-std = true
xargo = false
zig = true
default-target = "x86_64-unknown-linux-gnu"
pre-build = ["apt-get update && apt-get --assume-yes install libudev-dev"]

[build.env]
volumes = ["/taxon=../taxon"]
